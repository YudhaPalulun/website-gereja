import { defineConfig } from 'astro/config';

// Ganti "site" dengan alamat website gereja kamu setelah punya domain,
// ini membantu Astro membuat sitemap & URL kanonik yang benar.
export default defineConfig({
  site: 'https://gereja-pelita-kasih.example',
});
